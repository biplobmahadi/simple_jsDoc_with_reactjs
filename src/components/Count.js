/**
 * A Test Component
 * @component
 * @example
 * where is that example man
 * @param {number} a 
 * @param {number} b 
 * @returns number
 */
function Count(a = 3, b = 5){
    return a + b;
}

export default Count;